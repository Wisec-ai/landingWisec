---
const { align = "center" } = Astro.props;
---

<div class:list={["mt-16", align === "center" && "text-center"]}>
  <h1 class="text-primary text-4xl lg:text-5xl font-bold lg:tracking-tight">
    <slot name="title">Wisec.<span class="text-secondary">ai</span></slot>
  </h1>
  <p class="text-lg mt-4 text-primary">
    <slot name="desc">Impulsamos el diseño y construcción con IA</slot>
  </p>
</div>
